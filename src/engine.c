// 13519214

#include "engine.h"

/*
Useful UTF-8 Char for map
\u2588 -> Black Block
*/

// Core engine implementation
void draw(){
    system(CLSCRN);
    fflush(stdout);
    // dp(wall);
    // sleep(1);
}
